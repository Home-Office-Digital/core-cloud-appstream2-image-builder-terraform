{
  "schemaVersion": "2.2",
  "description": "Minimal fixture document for terraform test — not the real automation.json",
  "mainSteps": [
    {
      "action": "aws:runShellScript",
      "name": "Noop",
      "inputs": {
        "runCommand": ["echo test fixture"]
      }
    }
  ]
}
