{
  "Comment": "Test AppStream image automation",
  "StartAt": "Complete",
  "States": {
    "Complete": {
      "Type": "Pass",
      "Parameters": {
        "ssmDocNamePath.$": "$.SSMDocumentName",
        "roleArn": "${AppStreamInstanceRoleArn}",
        "liveAccountId": "${LiveAccountId}",
        "preliveAccountId": "${PreliveAccountId}"
      },
      "End": true
    }
  }
}